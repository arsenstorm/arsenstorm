// Injected by Vite `define` at build time — see vite.config.ts.
// Content hashes used as cache keys for the rendered CV PDF and OG images.
declare const __CV_CONTENT_HASH__: string;
declare const __OG_TEMPLATE_HASH__: string;
