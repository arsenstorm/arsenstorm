---
export const prerender = true;

import { ExperienceList } from "#/components/experience-list.tsx";
import Main from "#/components/main.astro";
import { PageHeading } from "#/components/page-heading.tsx";
import { Section } from "#/components/section.tsx";
import BaseLayout from "#/layouts/base-layout.astro";
import { getExperience } from "#/lib/experience";

const TITLE = "My Experience";
const DESCRIPTION = "Where I've worked and what I did there.";
const META_DESCRIPTION =
	"Work experience of Arsen Shkrumelyak, freelance software engineer: previously founding engineer at Anyverse and sole engineer at Afinity, building AI-assisted admissions products.";

const experience = await getExperience();
---

<BaseLayout description={META_DESCRIPTION} path="/experience" title={TITLE}>
	<Main>
		<PageHeading description={DESCRIPTION} title={TITLE} />

		<Section cta={{ label: "Go back", href: "/" }} title="Experience">
			<ExperienceList items={experience} />
		</Section>
	</Main>
</BaseLayout>
