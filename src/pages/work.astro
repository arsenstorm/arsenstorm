---
export const prerender = true;

import { ItemGroup } from "#/components/item-group.tsx";
import { PageHeading } from "#/components/page-heading.tsx";
import { Section } from "#/components/section.tsx";
import BaseLayout from "#/layouts/base-layout.astro";
import { PROJECTS } from "#/lib/projects.ts";

const TITLE = "My Work";
const DESCRIPTION = "These are the things I've worked on.";
---

<BaseLayout description={DESCRIPTION} path="/work" title={TITLE}>
	<main class="mx-auto max-w-xl space-y-12 px-6 py-24">
		<PageHeading description={DESCRIPTION} title={TITLE} />

		<Section cta={{ label: "Go back", href: "/" }} title="All of my work">
			<ItemGroup id="projects-list" items={PROJECTS} />
		</Section>
	</main>
</BaseLayout>
